kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: data-cassandra
  namespace: spinnaker
  labels:
    app: data
    stack: cassandra
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi
