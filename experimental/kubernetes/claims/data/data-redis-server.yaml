kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: data-redis-server
  namespace: spinnaker
  labels:
    app: data
    stack: redis
    detail: server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
